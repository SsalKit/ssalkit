<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\SsalKit.Guard\SsalKit.Guard.csproj" />
    <!-- Referenced as an analyzer so the [ErrorCodes] demo below runs against the real
         source generator without having to pack and consume the NuGet package first. -->
    <ProjectReference Include="..\..\src\SsalKit.Guard.Generator\SsalKit.Guard.Generator.csproj"
                      OutputItemType="Analyzer"
                      ReferenceOutputAssembly="false" />
  </ItemGroup>

</Project>
