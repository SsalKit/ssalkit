<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\SsalKit.Guard\SsalKit.Guard.csproj" />
    <!-- Consume the generator the way a NuGet consumer would (as an analyzer), without
         having to pack first: the tests below call the real generated mapping table and
         the real generated factory/throw helpers. -->
    <ProjectReference Include="..\..\src\SsalKit.Guard.Generator\SsalKit.Guard.Generator.csproj"
                      OutputItemType="Analyzer"
                      ReferenceOutputAssembly="false" />
  </ItemGroup>

</Project>
